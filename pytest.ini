[pytest]
# Default options for all pytest runs in this repo
# -s: show stdout/stderr live
# -vv: very verbose output
# --basetemp: base directory where pytest creates temp dirs (used by tmp_path)
addopts = -s -vv -p no:dash

# Discover tests only under the tests/ folder
testpaths = tests

