"""Assets pairing bounded context (DDD layered package).

This package is intentionally split into:
- domain: pure domain model + domain services
- application: use-cases (orchestration)
- infrastructure: IO adapters (filesystem, etc.)
"""
